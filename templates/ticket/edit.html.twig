{% extends 'base.html.twig' %}

{% block title %}Edit Ticket{% endblock %}

{% block body %}
<div class="container">
<h1 class="text-center mt-5 mb-3"><font color="white">Edit Ticket</font></h1>
    <body style="background-color:#483D8B;"></body>
      <div class="card">
         <div class="card-header">
     <center>
     <center><img src="{{ asset('super.png') }}" alt="Symfony!"/> </center>


    {{ include('ticket/_form.html.twig', {'button_label': 'Update'}) }}

     <div class="card-header">
    <a class="btn btn-outline-info float-right" href="{{ path('app_ticket_index') }}">back to list</a>

    {{ include('ticket/_delete_form.html.twig') }}
    </div>
     </div>
     </div>
      </center>
{% endblock %}
