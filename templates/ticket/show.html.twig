{% extends 'base.html.twig' %}

{% block title %}Ticket{% endblock %}

{% block body %}
  <div class="container">
    <h1 class="text-center mt-5 mb-3"><font color="white">Ticket</font></h1>
     <body style="background-color:#483D8B;"></body>
     <div class="card">
     <div class="card-header">
      <center><img src="{{ asset('super.png') }}" alt="Symfony!"/> </center>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ticket.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ ticket.codigo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ ticket.descripcion }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ ticket.cantidad }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ ticket.precio }}</td>
            </tr>
        </tbody>
    </table>
   
    <a class="btn btn-outline-info float-right" href="{{ path('app_ticket_index') }}">back to list</a>

    <a class="btn btn-outline-info float-right" href="{{ path('app_ticket_edit', {'id': ticket.id}) }}">edit</a>

    {{ include('ticket/_delete_form.html.twig') }}
      </div>
     </div>
     </div>
{% endblock %}
